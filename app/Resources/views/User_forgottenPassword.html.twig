{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <h2 class="text-center">Forgotten password</h2>
            <div class="col-md-2 col-md-offset-5 text-center whiteBackground">
                <form action="{{ path('forgotpassword') }}" method="post" class="form-horizontal">
                    <div class="form-group text-center">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        {{ form_end(form) }}
                    </div>
                </form>
            </div>
        </div>
    </div>

{% endblock %}